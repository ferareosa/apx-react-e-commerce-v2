{
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "../dist/client",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!.*\\..*|_next).*)",
      "destination": "/"
    }
  ]
}
